README

Uses:
* Leaflet.js
* jQuery

This project demonstrates the use of map tiles and the Leaflet.js library for displaying offline maps in the Web browser.

The maps included with this project support only one zoom level. However, these can be replaced by files prepared with the Mobile Atlas Creator.

